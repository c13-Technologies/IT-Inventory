{
  "name": "clarity-inventory",
  "version": "1.0.0",
  "private": true,
  "description": "Clarity — Inventory & Management admin dashboard served via Express. EJS templates render server-side against a Prisma/Postgres backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d && node scripts/wait-for-pg.js && npx prisma db push",
    "db:logs": "docker compose logs -f db",
    "db:psql": "docker compose exec db psql -U postgres -d inventory_dev",
    "db:wait": "node scripts/wait-for-pg.js",
    "db:seed": "npx prisma db seed"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "ejs": "^6.0.1",
    "express": "^4.19.2",
    "express-session": "^1.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^5.22.0"
  }
}
